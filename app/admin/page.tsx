import {products} from '@/lib/products';import {money} from '@/lib/store';export default function Admin(){const inventory=products.reduce((s,p)=>s+p.stock,0);const catalogValue=products.reduce((s,p)=>s+p.price*p.stock,0);return <main className="wrap page"><div className="pageIntro"><span className="eyebrow">LAPKA Admin</span><h1>Панель магазину</h1><p>Демо-адмінка. Перед production її потрібно закрити авторизацією.</p></div><div className="stats"><div><small>SKU</small><b>{products.length}</b></div><div><small>Залишок</small><b>{inventory}</b></div><div><small>Вартість каталогу</small><b>{money(catalogValue)}</b></div><div><small>Постачальники</small><b>3 ready</b></div></div><div className="card"><h2>Каталог</h2><div className="adminTable">{products.map(p=><div key={p.id}><span>{p.emoji} {p.brand} — {p.name}</span><b>{money(p.price)}</b><small>{p.stock} шт.</small></div>)}</div></div><div className="card integration"><h2>Інтеграції</h2><p><b>ZooBaza / Todli / COLLAR:</b> готовий API-шар для імпорту. Потрібні реальні XML/YML URL від постачальників.</p><p><b>Нова пошта:</b> endpoint підготовлений, для живого пошуку відділень потрібен <code>NOVA_POSHTA_API_KEY</code>.</p><p><b>Онлайн-оплата:</b> checkout готовий, платіжний провайдер активується після merchant-ключів.</p></div></main>}

export async function POST(req:Request){const key=process.env.NOVA_POSHTA_API_KEY;if(!key)return Response.json({configured:false,message:'Додайте NOVA_POSHTA_API_KEY у Vercel Environment Variables.'},{status:503});const body=await req.json();const r=await fetch('https://api.novaposhta.ua/v2.0/json/',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({apiKey:key,modelName:'Address',calledMethod:'getWarehouses',methodProperties:{CityName:body.city||'',FindByString:body.query||'',Limit:'50'}})});return Response.json(await r.json())}

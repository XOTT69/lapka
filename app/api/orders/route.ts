export async function POST(req:Request){try{const body=await req.json();if(!body?.customer?.name||!body?.customer?.phone||!Array.isArray(body?.items)||!body.items.length)return Response.json({error:'Invalid order'},{status:400});const orderId='LP-'+Date.now().toString().slice(-8);return Response.json({ok:true,orderId,status:'accepted'})}catch{return Response.json({error:'Bad request'},{status:400})}}

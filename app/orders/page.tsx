'use client';import Link from 'next/link';import {money,useStore} from '@/lib/store';export default function Orders(){const {orders}=useStore();return <main className="wrap page narrow"><div className="pageIntro"><span className="eyebrow">Історія</span><h1>Мої замовлення</h1></div>{orders.length?orders.map(o=><div className="card orderCard" key={o.id}><div><small>{new Date(o.createdAt).toLocaleString('uk-UA')}</small><h3>{o.id}</h3><span>{o.items} товарів • {o.status}</span></div><b>{money(o.total)}</b></div>):<div className="empty"><h2>Замовлень ще немає</h2><Link className="primary" href="/catalog">Почати покупки</Link></div>}</main>}

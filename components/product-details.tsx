'use client';
import {Heart,ShoppingBag,Truck,ShieldCheck,RotateCcw} from 'lucide-react';
import {Product} from '@/lib/products';
import {money,useStore} from '@/lib/store';
export default function ProductDetails({product}:{product:Product}){const {add,favorites,toggleFavorite,pet}=useStore();const liked=favorites.includes(product.id);return <div className="productDetails"><div className="productHero">{product.badge&&<i>{product.badge}</i>}<span>{product.emoji}</span></div><div className="productBody"><small>{product.brand} • {product.category}</small><h1>{product.name}</h1><p className="lead">{product.description}</p>{pet&&<div className="match">{product.pet===(pet.type==='Кіт'?'Коти':'Собаки')?'✓ Підходить для профілю '+pet.name:'ℹ Перевірте тип тварини в профілі'}</div>}<div className="bigPrice">{money(product.price)} {product.oldPrice&&<del>{money(product.oldPrice)}</del>}</div><div className="buyRow"><button className="primary grow" onClick={()=>add(product)}><ShoppingBag size={19}/>Додати в кошик</button><button className={'icon large '+(liked?'liked':'')} onClick={()=>toggleFavorite(product.id)}><Heart fill={liked?'currentColor':'none'}/></button></div><div className="facts">{product.age&&<div><b>Вік</b><span>{product.age}</span></div>}{product.weight&&<div><b>Фасування</b><span>{product.weight}</span></div>}<div><b>Залишок</b><span>{product.stock} шт.</span></div></div><h3>Переваги</h3><ul className="features">{product.features.map(x=><li key={x}>✓ {x}</li>)}</ul><div className="serviceRow"><span><Truck/>Нова пошта по Україні</span><span><ShieldCheck/>Офіційний товар</span><span><RotateCcw/>14 днів на повернення*</span></div></div></div>}
